from django.apps import AppConfig


class SecurityConfig(AppConfig):
    name = "hub.apps.security"
    label = "security"
    default_auto_field = "django.db.models.BigAutoField"
    verbose_name = "Security"
