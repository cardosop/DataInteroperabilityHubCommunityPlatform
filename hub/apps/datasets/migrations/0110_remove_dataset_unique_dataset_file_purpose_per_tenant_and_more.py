# Generated by Django 6.0 on 2026-05-25 19:34

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("datasets", "0109_remove_dataset_unique_dataset_file_purpose_per_tenant_and_more"),
    ]

    operations = [
        migrations.RemoveConstraint(
            model_name="dataset",
            name="unique_dataset_file_purpose_per_tenant",
        ),
        migrations.RemoveIndex(
            model_name="dataset",
            name="datasets_tenant__f0c0c8_idx",
        ),
        migrations.AlterField(
            model_name="dataset",
            name="file_handle_purpose",
            field=models.CharField(
                choices=[
                    ("PRIMARY", "Primary"),
                    ("SAMPLE", "Sample"),
                    ("SCHEMA_ONLY", "Schema Only"),
                ],
                db_index=True,
                default="PRIMARY",
                help_text="Phase 260.5.A — disambiguates concurrent Dataset rows backed by the same File. The (tenant, file, file_handle_purpose) unique constraint (when file is non-null) lets a tenant have AT MOST ONE PRIMARY + AT MOST ONE SAMPLE + AT MOST ONE SCHEMA_ONLY dataset per file.",
                max_length=20,
            ),
        ),
        migrations.AlterField(
            model_name="dataset",
            name="kind",
            field=models.CharField(
                choices=[
                    ("FILE", "File-based dataset"),
                    ("EXTERNAL_REF", "External reference dataset"),
                ],
                db_index=True,
                default="FILE",
                help_text="Phase 260.6.C — distinguishes file-backed datasets (FILE) from external-reference datasets (EXTERNAL_REF). Per OQ260.3 the API write surface accepts only FILE from external clients; EXTERNAL_REF is set server-side by the federation pipeline.",
                max_length=20,
            ),
        ),
        migrations.AlterField(
            model_name="datasetsnapshot",
            name="snapshot_type",
            field=models.CharField(
                choices=[
                    ("FULL", "Full"),
                    ("SCHEMA_ONLY", "Schema Only"),
                    ("METADATA_ONLY", "Metadata Only"),
                    ("INCREMENTAL", "Incremental"),
                ],
                default="FULL",
                help_text="Snapshot type: FULL, SCHEMA_ONLY, METADATA_ONLY",
                max_length=20,
            ),
        ),
    ]
