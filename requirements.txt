# Core Django Framework
Django>=6.0,<7.0
djangorestframework>=3.15.0
django-cors-headers>=4.0.0

# Database
psycopg2-binary>=2.9.9

# Job Queue
django-rq>=3.2.0
rq>=1.15.0
redis>=5.0.0

# GraphQL
strawberry-graphql[django]>=0.287.0
graphene-django>=3.2.0

# Authentication & Security
PyJWT>=2.8.0
cryptography>=41.0.0
django-environ>=0.11.0

# Object Storage (S3-compatible)
boto3>=1.34.0
django-storages>=1.14.6

# Observability
structlog>=24.1.0
django-structlog>=10.0.0
# prometheus-client>=0.20.0  # Replaced by OpenTelemetry metrics
# django-prometheus>=2.4.0  # Removed: Not compatible with Django 6.0
deprecated>=1.2.0  # Required by OpenTelemetry packages
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-django>=0.42b0
opentelemetry-instrumentation-requests>=0.42b0  # Required for HTTP request tracing
opentelemetry-instrumentation-httpx>=0.42b0  # Required for HTTPX client tracing
opentelemetry-exporter-jaeger>=1.20.0
opentelemetry-exporter-otlp>=1.20.0  # OTLP exporter (preferred over Jaeger)
opentelemetry-exporter-prometheus>=0.60b0

# Error Tracking
sentry-sdk>=2.0.0  # Optional: Error tracking with Sentry (set SENTRY_DSN to enable)

# Typed models and validation
pydantic>=2.6,<3.0

# Data Quality Engines
great-expectations>=0.18.0
soda-core>=3.0.0

# Data Processing
pandas>=2.1.0
numpy>=1.26.0

# HTTP Client
httpx>=0.25.0
requests>=2.31.0

# Email Services
sendgrid>=6.10.0
boto3>=1.34.0  # Already included for S3, also used for SES

# RDF/Semantic
rdflib>=7.0.0
SPARQLWrapper>=2.0.0

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3
pyyaml>=6.0.1
html2text>=2024.2.26
click>=8.0.0  # CLI framework
croniter>=2.0.0  # Cron expression parsing for scheduled ingestions

# OpenAPI/Swagger
drf-spectacular>=0.27.0

# WebSocket Support
channels>=4.0.0
channels-redis>=4.2.0
django-filter>=23.0
