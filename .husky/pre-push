#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run type checking and linting for frontend before push
if git diff --name-only origin/main...HEAD | grep -q "^frontend/"; then
  echo "Running frontend type check and lint..."
  cd frontend
  npm run type-check
  npm run lint
fi

