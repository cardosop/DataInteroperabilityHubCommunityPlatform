openspec/
InputDocs/
AGENTS.md
.cursor/

# MkDocs build output
site/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
pip-log.txt
pip-delete-this-directory.txt

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/media
/staticfiles
/static

# Environment (never commit files that may contain secrets)
.env
.env.local
.env.*.local
.env.test
.env.test.ckan
.env.e2e
.gcp-sa-test.json
!.env.example
!.env.test.example

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
# Test evidence and reports (generated by run_phase_12a_* and collect_test_evidence.sh)
test_reports_comprehensive/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker volumes
pgdata/
redis-data/
minio-data/
fuseki-data/

# Build artifacts
*.egg-info/
dist/
build/

# Documentation
docs/_build/

# Temporary files
*.tmp
*.bak
*.swp

# Unix core dumps (pattern must not match hub/apps/core/)
core.*
/core

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
terraform.tfvars
.terraform.lock.hcl

# Playwright test artifacts
frontend/test-results/
frontend/playwright-report/

# AWS CLI installer artifacts
awscliv2.zip
aws/

# Pip/npm empty version-specifier junk files at repo root (e.g. =1.2.3)
=*.*.*
=3.8

# Phase 216 — performance test results (CI uploads to S3)
.perf-results/
node_modules

# Phase 227 Wave 0 — operator-generated diagnosis artefacts.
# Contains tenant + contract IDs from staging-clone or production runs.
# Keep README.md tracked (instructions); ignore everything else.
audit-reports/*
!audit-reports/README.md

# Docker buildkit progress lines accidentally captured to files when
# `docker compose ... > some-file` runs without proper redirection.
# Phase 228 cleanup — zero-byte files that keep getting recreated
# during interactive compose-from-repo-root sessions.
CACHED
transferring
[builder
[internal]
[stage-1

# Playwright test result captures (`npx playwright test --reporter line
# > tests-results.txt`). Belong in CI artifact paths, never the repo.
test-artifacts/
/frontend/tests-results*.txt
/frontend/frontend/

# Frontend guard / drift tooling output (Phase 228 lineage hardening).
/frontend/guard-failures/
