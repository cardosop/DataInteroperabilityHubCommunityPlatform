# Bandit YAML configuration (alternative format)
# See: https://bandit.readthedocs.io/en/latest/config.html

exclude_dirs:
  - .git
  - .venv
  - venv
  - __pycache__
  - .pytest_cache
  - .mypy_cache
  - build
  - dist
  - migrations
  - node_modules
  - tests

skips:
  - B101  # assert_used - Allow asserts in non-test production code (rare but intentional)

tests: []

severity_level: low
confidence_level: low

